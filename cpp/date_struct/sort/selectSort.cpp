#include<iostream>
using namespace std;

void selectSort(int *arr, int len)
{
    if (NULL == arr || len <= 0)
        return;

    for (int i = =

}

int main(void)
{
    int arr[] = {0,8,6,4,2,1,3,5,7,9};
    int len = sizeof(arr)/sizeof(arr[0]);

    return 0;
}
