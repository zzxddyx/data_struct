#include <iostream>
#include <iterator>
using namespace std;

/*
int &get(int *arr, int index)
{
    return arr[index];
}

int main(void)
{
    int arr[10];
    for (auto i = 0; i != 10; ++i)
    {
        get(arr, i) = i;
    }

    int len = sizeof(arr) / sizeof(arr[0]);
    for (int i = 0; i < len; ++i)
    {
        cout << arr[i] << endl;
    }
    
    return 0;
}
*/

void print(vector<int> &arr)
{
    if (arr.size() != 1)
    {

    }
}

int main(void)
{
    return 0;
}
